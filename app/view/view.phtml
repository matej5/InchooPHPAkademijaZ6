<div class="post">
    <?php if(!empty( $post->getImage() )){ ?>
    <div class="image">
        <img src="<?php echo App::config('url') ?>app/images/<?= $post->getImage() ?>" alt="" />
    </div>
    <?php } ?>
    <div class="content">
        <h3><?= $post->getDateCreated() ?></h3>
        <p><?= htmlspecialchars($post->getContent()) ?></p>

        <form action="<?php echo App::config('url') ?>Index/uploadImage/<?= $post->getId() ?>" method="post" enctype="multipart/form-data">
            <div>
                <h4>Select image to upload:</h4>
                <input type="file" name="fileToUpload" id="fileToUpload">
                <input type="submit" value="Upload Image" name="submit">
            </div>
        </form>
        <form action="<?php echo App::config('url') ?>Index/deletePost/<?= $post->getId() ?>" method="post" enctype="multipart/form-data">
            <div>
                <input type="submit" value="Delete Post" name="submit">
            </div>
        </form>
    </div>
</div>

<h3>Comments</h3>
<table class="table-expand">
    <thead>
    <tr class="table-expand-row">
        <th width="200">Date</th>
        <th>Content</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($comments as $comment): ?>
        <tr class="table-expand-row" data-open-details>
            <td><?= $comment->getDateCreated()?></td>
            <td><?= htmlspecialchars($comment->getContent()) ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>